# Аудит репозитория `Serg121440/Univer`

Дата аудита: 2026-05-19 (UTC)

## Краткий итог

Репозиторий в текущем состоянии содержит только один файл (`README.md`) и не содержит исходного кода backend/frontend, Docker-конфигов и инфраструктурных файлов, заявленных в README.

## Детальные комментарии к исправлениям

### 1) Несоответствие README фактическому содержимому
- В README указано, что реализованы FastAPI API, JWT auth, CRUD, Docker Compose и PostgreSQL schema.
- По факту в репозитории отсутствуют файлы приложения (`main.py`, `app/`, `frontend/`, `docker-compose.yml`, `Dockerfile`, миграции и т.д.).

**Что исправить:**
1. Либо загрузить фактический исходный код и инфраструктуру, соответствующие описанию.
2. Либо обновить README до реального состояния (например: "подготовлен только проектный план / спецификация").

### 2) Отсутствие минимально необходимых инженерных артефактов
В репозитории отсутствуют:
- `docker-compose.yml`
- зависимости (`requirements.txt`, `pyproject.toml`, `package.json`)
- `.env.example`
- CI-конфигурация (`.github/workflows/*`)
- тесты (`tests/`)

**Что исправить:**
1. Добавить базовую структуру проекта.
2. Добавить инструкцию "как запустить локально без Docker".
3. Добавить автопроверки (lint/test) в CI.

### 3) Неполная и потенциально вводящая в заблуждение документация
README содержит список endpoint’ов, но нет:
- схем запросов/ответов
- ролей и модели прав (student/teacher/admin)
- описания статусов ошибок

**Что исправить:**
1. Добавить OpenAPI ссылку/экспорт.
2. Добавить раздел "Контракты API" с примерами JSON.
3. Добавить таблицу ролей и разрешений.

### 4) Отсутствие критериев готовности MVP
Нет явных критериев "MVP готов" (DoD), что повышает риск разночтений.

**Что исправить:**
1. Добавить Definition of Done для backend/frontend.
2. Добавить список user stories с приоритетами.

### 5) Отсутствие базовой операционной документации
Для production-подобного запуска не хватает:
- политики логирования
- политики резервного копирования БД
- инструкций по миграциям
- observability (healthchecks/readiness/metrics)

**Что исправить:**
1. Добавить runbook (`docs/runbook.md`).
2. Добавить раздел "эксплуатация" в README.

## Приоритетный план исправлений

1. **P0:** Привести README в соответствие с фактическим состоянием **или** загрузить обещанный код.
2. **P0:** Добавить `docker-compose.yml` + backend skeleton + dependency manifests.
3. **P1:** Добавить `.env.example`, CI pipeline, базовые тесты.
4. **P1:** Формализовать API-контракты и модель ролей.
5. **P2:** Добавить runbook и эксплуатационные практики.

## Проверка, выполненная в рамках аудита
- Проверен список файлов репозитория.
- Подтверждено наличие только `README.md`.
